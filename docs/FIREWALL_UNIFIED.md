# 🔥 Firewall Rules - Unified Management

## نظرة عامة

تم دمج جميع قواعد الحماية (IP Rules, URL Rules, Country Rules) في شاشة واحدة تسمى **Firewall** لتسهيل الإدارة.

## المميزات

1. **شاشة موحدة**: جميع القواعد في مكان واحد
2. **اختيار نوع القاعدة**: عند إنشاء قاعدة جديدة، يمكنك اختيار نوعها (IP, URL, أو Country)
3. **عرض موحد**: جدول واحد يعرض جميع القواعد مع تمييز نوع كل قاعدة
4. **تصفية حسب الموقع**: يمكنك تصفية القواعد حسب الموقع (Global, All, أو موقع محدد)

## كيفية الاستخدام

### الوصول إلى شاشة Firewall

1. بعد تسجيل الدخول، اذهب إلى القائمة الجانبية
2. اضغط على **"🔥 Firewall Rules"** في قسم "Firewall & Sites"

### إضافة قاعدة جديدة

1. في شاشة Firewall، اختر نوع القاعدة من التبويبات:
   - **IP Rules**: لإضافة/حظر عناوين IP
   - **URL Rules**: لحماية مسارات URL معينة
   - **Country Rules**: لحظر/السماح بدول معينة

2. املأ البيانات المطلوبة حسب نوع القاعدة:
   
   **لـ IP Rules:**
   - IP Address: عنوان IP (مثال: 137.59.230.231)
   - Type: Allow (Whitelist) أو Block (Blacklist)
   - Site: اختياري - اتركه فارغاً للقاعدة العامة

   **لـ URL Rules:**
   - Name: اسم القاعدة (اختياري)
   - Host: اسم النطاق (اختياري)
   - Path: المسار المراد حمايته (مثال: /admin)
   - Allowed IPs: عناوين IP المسموح لها بالوصول (مفصولة بفواصل)

   **لـ Country Rules:**
   - Country Code: رمز الدولة (حرفين، مثال: SA, US, CN)
   - Type: Allow أو Block
   - Site: اختياري - اتركه فارغاً للقاعدة العامة

3. اضغط على زر "Add [Type] Rule"

### حذف قاعدة

1. في الجدول، ابحث عن القاعدة المراد حذفها
2. اضغط على زر **"Delete"** بجانب القاعدة
3. أكد الحذف

### تصفية القواعد حسب الموقع

استخدم التبويبات في أعلى الصفحة:
- **Global Rules**: القواعد العامة (للمشرفين فقط)
- **All Rules**: جميع القواعد
- **[Site Name]**: القواعد الخاصة بموقع معين

## الملفات المُنشأة/المُعدلة

### ملفات جديدة:
- `app/Http/Controllers/FirewallController.php` - Controller موحد لإدارة جميع القواعد
- `resources/views/waf/firewall.blade.php` - شاشة Firewall الموحدة
- `docs/FIREWALL_UNIFIED.md` - هذا الملف

### ملفات معدلة:
- `routes/web.php` - إضافة routes جديدة للـ Firewall
- `resources/views/layouts/waf.blade.php` - تحديث القائمة الجانبية

## التوافق مع الكود القديم

تم الاحتفاظ بالـ routes القديمة للتوافق مع الكود الموجود:
- `/waf/ip-rules` - لا يزال يعمل
- `/waf/url-rules` - لا يزال يعمل
- `/waf/country-rules` - لا يزال يعمل

يمكنك استخدام أي من الطريقتين (القديمة أو الجديدة).

## ملاحظات مهمة

1. **الصلاحيات**: 
   - فقط المشرفون (Super Admins) يمكنهم إنشاء قواعد عامة (Global)
   - المستخدمون العاديون يمكنهم فقط إنشاء قواعد لمواقعهم

2. **المزامنة التلقائية**: 
   - جميع التغييرات يتم مزامنتها تلقائياً مع ModSecurity و Nginx
   - لا حاجة لإعادة تشغيل الخادم يدوياً

3. **الإحصائيات**: 
   - في أعلى الصفحة، يمكنك رؤية إحصائيات سريعة:
     - إجمالي القواعد
     - عدد قواعد IP
     - عدد قواعد URL
     - عدد قواعد الدول

## الخطوات التالية (اختياري)

إذا أردت إزالة الروابط القديمة من القائمة الجانبية:
1. افتح `resources/views/layouts/waf.blade.php`
2. احذف الروابط القديمة (IP Rules, URL Rules, Country Rules)
3. احتفظ فقط برابط "Firewall Rules"

---

**تم الإنشاء بواسطة**: AI Assistant  
**التاريخ**: {{ date('Y-m-d') }}

